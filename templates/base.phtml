<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <?php if (isset($css)):?>
        <?= $css;?>
    <?php endif;?>
    <title>Mon Super Blog </title>
</head>
<body>
    <header>
        <h1><?= $titlePage;?></h1>
        <nav>
            <a href="<?=buildUrl('home');?>">Accueil</a>
            <?php
                if (isConnected()) { 
                    ?>
                        <a href="<?=buildUrl('logout');?>">
                        <i class="fa-solid fa-arrow-right-from-bracket"></i> Déconnexion</a>
                         <p> Bonjour <?=htmlspecialchars($_SESSION['user']['firstname']);?>  <?=$_SESSION['user']['lastname'];?> </p>
                    <?php    
                } else {
                    ?>
                    <a href="<?=buildUrl('login');?>">Connexion</a>
                    <a href="<?=buildUrl('signup');?>">Créer un compte</a>
                    <?php 
                }
            ?>
        </nav>
    </header>
    <main>
        <?php
            include '../templates/'.$template.'.phtml';
        ?>
    </main>
</body>
</html>